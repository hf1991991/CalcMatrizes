[
    "
        Multiply(
            168;
            Elevate(
                Add(
                    Elevate(Add(5a;-8);-1);
                    -15a^{2}
                    -24
                    39a
                );
                -1
            )
        )
    ", 
    "
        Multiply(
            -177a;
            Elevate(
                Add(
                    Elevate(Add(5a;-8);-1);
                    -15a^{2}
                    -24
                    39a
                );
                -1
            )
        )
    ", 
    "
        Multiply(
            45a^{2};
            Elevate(
                Add(
                    Elevate(Add(5a;-8);-1);
                    -15a^{2}
                    -24
                    39a
                );
                -1
            )
        )
    ", 
    "
        Multiply(
            Elevate(Add(5a;-8);-1);
            Elevate(
                Add(
                    Elevate(Add(5a;-8);-1);
                    -15a^{2}
                    -24
                    39a
                );
                -1
            )
        )
    "
]